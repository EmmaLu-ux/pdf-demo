{
  "name": "pdf-demo-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "pnpm --filter pdf-demo-web dev",
    "build": "pnpm --filter pdf-demo-web build",
    "preview": "pnpm --filter pdf-demo-web preview",
    "lint": "pnpm --filter pdf-demo-web lint",
    "format": "pnpm --filter pdf-demo-web format",
    "test": "pnpm -r test"
  }
}
